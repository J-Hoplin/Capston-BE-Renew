export enum Sex {
  MALE = 'male',
  FEMALE = 'female',
}

export enum Role {
  STUDENT = 'student',
  INSTRUCTOR = 'instructor',
  MANAGER = 'manager',
}

export enum Approve {
  PENDING = 'pending',
  APPROVE = 'approve',
  REJECT = 'reject',
  RESTRICT = 'restrict',
}
